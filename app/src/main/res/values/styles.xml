<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <style name="normal_edittext" parent="android:TextAppearance.DeviceDefault.Widget.EditText">
        <item name="android:fontFamily">@font/sf_ui_display_regular</item>
        <item name="android:paddingLeft">@dimen/margin_size_10dp</item>
        <item name="android:paddingRight">@dimen/margin_size_10dp</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:layout_height">@dimen/margin_size_40dp</item>
        <item name="android:textColor">@color/colorEtText</item>
        <item name="android:textColorHint">@color/colorEtHint</item>
        <item name="android:layout_marginTop">@dimen/margin_size_10dp</item>
        <item name="android:textSize">@dimen/text_size_14sp</item>
        <item name="android:drawablePadding">@dimen/margin_size_10dp</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

        <style name="productRatingBar" parent="@android:style/Widget.RatingBar">
            <item name="android:minHeight">22dip</item>
            <item name="android:maxHeight">22dip</item>
        </style>

</resources>
